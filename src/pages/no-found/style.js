import styled from "styled-components";

export const NoFoundWrapper = styled.div`
  height: 1000px;

  h2 {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);

    a {
      color: lightblue;
    }
  }
`